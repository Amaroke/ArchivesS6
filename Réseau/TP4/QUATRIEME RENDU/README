Est-ce que client3 est compatible avec server2 ? Justifier.

client3 est compatible avec serveur2, mais seul le premier message envoyé par client3 est pris en compte par serveur2.

Est-ce que client2 est compatible avec server3 ? Justifier.

client2 n'est pas compatible avec server3 car server3 attends la réception de 2 messages et client2 n'en envoit qu'un seul.

Est-ce que client3 est compatible avec server2 ? Justifier 

client3 est compatible avec serveur2 car peu importe si les envoi se font d'une seule fois ou non ils sont tous reçu grâce à la fonction.

